using UnityEngine;

public class WinkData : MonoBehaviour
{
    /*--- Properties ---*/
    public bool face_detected = false;
    public bool left_wink = false;
    public bool right_wink = false;


    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
